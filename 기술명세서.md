# 전원마을 파인더 — 기술 명세서 (Technical Specification)

> **문서 성격**: 내부 엔지니어링 빌드 설계도 (제출용 아님). 심사 제출은 `MVP제안서.md` 참조.
> **연계**: `../한국농어촌공사대회_우승전략_심화재설계_v3.md`의 확정 방향(물-점수 분리 / 분양 실시간 1과업 / KRC 4대 영역)을 기술적으로 구현.
> **작성 기준일**: 2026-07-11 · **대상 공모전**: 제3회 KRC AI 디지털 혁신 공모전 서비스개발 부문 (마감 2026-07-31)
> **참조 시스템 실측 반영**: AITHOR-Agent-Framework / minicpm5-poc / AI-research-SKILLs (아래 §12 매핑)

---

## 0. 문서 범위와 설계 원칙

### 0.1 범위
자연어 조건 입력 → KRC 공공데이터 실시간 조회 → **"지금 분양 가능한 전원마을" Top N** + 지역 농업가뭄 참고 패널 + 환각 방지 근거 리포트를 제공하는 웹 서비스의 기술 설계.

### 0.2 설계 원칙 (v3 + Codex 교차검증 반영)
1. **결정론 우선, LLM 최소** — 점수·API 호출·필터는 결정론적 코드. LLM은 자연어→구조화 파싱 + 설명 생성만 (SP#18 "Retrieval/Computation vs Reasoning 분리").
2. **인용 없는 값은 출력 금지** — 모든 수치는 API 응답 필드에 바인딩. 미바인딩 = 렌더 차단 (환각 0).
3. **물은 점수에서 분리** — 저수율/가뭄단계는 시군 참고 패널로만. 마을 점수에 미포함 (인과 오류 제거).
4. **무저장 기본 / 최소 수집** — 개인식별정보 입력 차단, 외부 전송 최소화 (프라이버시 아키텍처 첫날 확정).
5. **3주 내 완성 가능한 최소 스택** — 무거운 프레임워크 전체 도입 금지. 검증된 **패턴만** 경량 이식.

### 0.3 명시적 비목표 (Non-Goals)
- 실제 계약·투자·분양 확정 판단 (참고정보만 제공).
- 저수지↔마을 수혜구역 정밀 매칭 (수혜구역 데이터 부재 — v3 결정).
- 예측 모델 기반 가뭄/분양 예측 (설명 가능한 공식 지표만).

---

## 1. 시스템 개요

### 1.1 컨텍스트 다이어그램
```
[사용자(귀농 준비자)]
      │ 자연어 조건("충남, 예산 2억, 분양 진행 중인 조용한 마을")
      ▼
┌─────────────────── 전원마을 파인더 (웹) ───────────────────┐
│  프론트엔드(3화면)  ──►  백엔드 오케스트레이터              │
│                            │                                │
│   ┌────────────────────────┼───────────────────────────┐   │
│   │ Intent(파싱)  Data(조회)  Score(결정론)  Verify(근거) │   │
│   └────────────────────────┼───────────────────────────┘   │
└────────────────────────────┼───────────────────────────────┘
                             ▼  (HTTPS, allowlist)
          [공공데이터포털 KRC OpenAPI 4종] + [샘플 캐시/fallback]
```

### 1.2 핵심 사용자 흐름
```
1. 조건 입력(자연어 + 칩)  → Intent Agent: NL → 구조화 조건(JSON)
2. Data Layer: 전원마을 분양정보 API로 조건 매칭 지구 조회 (진행단계/분양율)
3. Score: 결정론적 적합도 계산 (물 제외)
4. 보조: 농촌마을현황(마을 기본정보) + 논가뭄지도(시군 가뭄 패널)
5. Verify: 각 값을 API 필드에 바인딩 → 근거 리포트 생성 (미바인딩 차단)
6. 응답: Top 3 카드 + 근거 리포트 + 지역 가뭄 패널
```

---

## 2. 아키텍처

### 2.1 계층 구조 (AITHOR `kernel.py` 파이프라인 패턴 차용)

> AITHOR `src/aithor_agent_framework/kernel.py`의 `AgentKernel.run`: **input guard → domain steps → budget gate → oracle → adversarial → output guard → audit**. 이 골격을 MVP 규모로 경량 이식.

```
요청
 │
 ▼ [L1 Input Guard]      PII 리댁션 + prompt injection 차단 (AITHOR guard.py 패턴)
 ▼ [L2 Intent Parser]    NL → 구조화 조건 (LLM, structured output)
 ▼ [L3 Data Orchestrator] KRC API 4종 호출 + 캐시/fallback (결정론)
 ▼ [L4 Scoring]          적합도 점수 계산 (결정론, 물 제외)
 ▼ [L5 Evidence/Verify]  값↔API필드 바인딩, 미바인딩 claim 차단 (AITHOR verifier.py 패턴)
 ▼ [L6 Explanation]      추천 설명 생성 (LLM, 바인딩된 필드만 참조)
 ▼ [L7 Output Guard]     출력 PII/시크릿 리댁션 + "참고정보·최종확인 필요" 고지 부착
 ▼ [L8 Audit Log]        호출/응답시간/fallback/신뢰도 로그 (관측성)
응답
```

### 2.2 컴포넌트 책임

| 컴포넌트 | 책임 | 구현 방식 | AI 여부 |
|---|---|---|:---:|
| Input Guard | PII 차단·injection 방어 | 결정론 (정규식, allowlist) | ✗ |
| Intent Parser | 자연어→구조화 조건 | LLM + structured output | ✓ |
| Data Orchestrator | KRC API 호출·정규화·캐시 | 결정론 (HTTP client) | ✗ |
| Scoring Engine | 적합도 점수 | 결정론 (가중합) | ✗ |
| Evidence Binder | 값↔필드 바인딩·검증 | 결정론 (스키마 검증) | ✗ |
| Explanation | 추천 이유 문장 | LLM (바인딩 필드만) | ✓ |
| Output Guard | 출력 정제·고지 | 결정론 | ✗ |

> **AI 최소화 원칙**: 7개 컴포넌트 중 LLM은 2개(파싱·설명)만. 나머지 5개는 결정론 → 환각 표면적 최소화 + AI 필요성 명확화(§10 평가로 증빙).

---

## 3. 데이터 계층

### 3.1 KRC 공공데이터 API 4종 (실측 스펙)

| API | data.go.kr ID | 역할 | 조회키 | 핵심 응답필드 | 갱신 | 승인 |
|---|---|---|---|---|---|---|
| **전원마을 분양정보** | 15104395 | 🟢 핵심 | 법정동코드/시도/시군구 | 사업관리번호·지구명·읍면동·**계획세대수·진행단계·분양율** | 실시간 | 개발 자동/운영 심의 |
| 농촌마을현황 | 15104291 | 보조(마을 기본) | 법정동코드/마을ID | 인구(연령·성별)·빈집수·마을유래·자연/경제자원 | 실시간 | 개발 자동/운영 심의 |
| 논가뭄지도 | 15117185 | 지역 가뭄 패널 | 시군코드 | 기준일자·저수율·평년·평년대비·**가뭄단계** | **연 1회** | 제한없음(CSV+API) |
| 저수지 수위정보 | 15099919 | 선택(실시간 저수율) | fac_code(+county) | water_level·rate·check_date | 실시간 | 자동(개발+운영) |

- **가뭄단계 기준(논가뭄지도 실측)**: 평년대비 ≤70% 관심 / ≤60% 주의 / ≤50% 경계 / ≤40% 심각.
- **데이터 형식**: 전원마을·농촌마을·저수지 = REST(JSON/XML), 논가뭄지도 = CSV+OpenAPI(XML/JSON).
- **트래픽**: 개발계정 일 10,000건 (MVP 시연 충분).
- ⚠️ `county` 조회키·응답 필드 매핑은 **[미확인]** → D1 실제 호출로 확정.

### 3.2 데이터 매칭 전략 (v3 확정)
- 전원마을·농촌마을 = **법정동코드 공통** → 직접 조인(인과 명확).
- 논가뭄지도 = **시군코드** → 지구의 시군으로 조인해 **패널에만** 표시 (마을 점수 미포함).
- 저수지 = fac_code+county 텍스트 → 좌표·법정동코드 없음 → **점수/직접매칭 금지**, 선택적 실시간 저수율 보조 표시만.

### 3.3 신뢰도 등급 (정직성 장치)
```
A: 법정동코드 직접 매칭 (전원마을↔농촌마을)
B: 시군 텍스트 매칭 (지구↔논가뭄지도)
C: 시군 대표 지표 참고 (지역 평균)
D: 데이터 부족 → "확인 불가" 표기, 점수 계산에서 제외
```
> Codex 교차검증 반영: 등급은 "정확도 과장"이 아니라 **매칭 방식의 투명한 표시**. A등급을 좌표매칭에 부여하지 않음(좌표 소스 부재).

### 3.4 캐시 & Fallback (발표 리스크 대비)
- **샘플 응답 캐시**: 4종 API의 대표 응답을 JSON/XML로 저장 (`data/samples/`).
- **Fallback 순서**: 실시간 호출 실패 → 캐시 응답 → "데이터 확인 불가" 표기.
- **발표 백업**: 로컬 실행본 + 오프라인 샘플 (네트워크/인증키 장애 대비).

---

## 4. AI / 자연어 파싱 계층

### 4.1 Intent Parser (NL → 구조화 조건)

**입력**: `"충남, 예산 2억, 분양 진행 중인 조용한 전원마을"`
**출력 스키마 (Pydantic, AI-research-SKILLs `16-prompt-engineering/instructor` 패턴)**:
```json
{
  "region": {"sido": "충청남도", "sigungu": null},
  "budget_max_krw": 200000000,
  "sale_stage": ["분양중"],
  "household_scale": null,
  "preferences": ["조용함"],
  "confidence": 0.0
}
```

### 4.2 모델 옵션 (2안 — 정직한 trade-off)

| 옵션 | 구성 | 장점 | 제약 | MVP 채택 |
|---|---|---|---|:---:|
| **A. 호스티드 소형 LLM** | 소형 모델(Haiku 4.5 / GPT 소형) + structured output (instructor/outlines) | 빠른 구현, 안정적 JSON | 외부 전송(고지 필요), 소액 비용 | ✅ 기본 |
| **B. 로컬 LLM** | MiniCPM5-1B(4-bit GGUF) via Ollama `127.0.0.1` | **완전 로컬(외부 전송 0)·저비용·프라이버시** | JSON 안정성 위해 **파인튜닝 필수**(GPU), 미파인튜닝시 json_valid 낮음 | 🔵 확장(프라이버시 모드) |

> **minicpm5-poc 실측 근거**: `harness/run_agent.py`가 Ollama HTTP(temp 0, stdlib)로 NL→`<tool_call>{json}` 파싱 구현. `convention.py format_tool_call/parse_strict`가 곧 NL→구조화 추출기. 단 `eval/report_base_cpu.json`상 **base 모델 json_valid 0.0** → 안정적 JSON엔 파인튜닝 필요. 따라서 MVP 기본은 A(호스티드), B는 "개인정보 무전송" 차별화용 확장 로드맵.

### 4.3 파싱 신뢰장치
- **structured output 강제**: instructor(Pydantic 재시도) 또는 outlines/guidance(문법 제약 생성)로 유효 JSON 보장.
- **저신뢰 파싱 처리**: `confidence < 0.6` → 사용자에게 조건 재확인 UI (환각 대신 되묻기).
- **미지원 질의**: 스키마 밖 요청 → "확인 불가" 정직 거부 (거부율을 §10에서 측정).

### 4.4 설명 생성 (Explanation)
- 입력: 바인딩된 API 필드값만. 출력: "이 지구는 **분양율 60%·진행단계 분양중**이라 추천" 형식.
- **하드 제약**: 프롬프트에 "제공된 필드값 외 수치·사실 생성 금지" + 출력 후 §5 검증 통과 필수.

---

## 5. 검증 / 환각 방지 계층 (핵심 차별점)

> AITHOR `src/aithor_agent_framework/verifier.py`(`verify_atomic_claims` + `EvidenceIndex`) + `evidence.py`(`EvidenceStack` claim↔span 바인딩) 패턴을 이식.

### 5.1 Evidence Binding
- 응답의 **모든 수치·사실 문장**을 원천 API 필드에 바인딩:
  ```
  claim: "분양율 60%"  →  evidence: {api: "전원마을분양정보", field: "분양율", value: 60, fetched_at: "..."}
  ```
- 바인딩 실패 claim = **렌더 차단** (AITHOR "미인용 claim = FAIL" 결정론적 규칙).

### 5.2 근거 리포트 스펙 (사용자 노출)
```
[근거 리포트]
- 사용한 KRC API: 전원마을 분양정보(15104395), 농촌마을현황(15104291)
- 추천 근거 필드: 진행단계=분양중, 분양율=60%, 계획세대수=120
- 지역 가뭄(참고): 논가뭄지도 시군 가뭄단계=주의 (기준일 2026-07-20, 연1회 갱신)
- 데이터 신뢰도: A(법정동코드 직접 매칭)
- 유의사항: 공공데이터 기반 참고정보이며 최종 계약·분양은 공식 기관 확인 필요
```

### 5.3 검증 파이프라인
```
생성된 응답 → schema_guard(구조 검증) → evidence_binder(필드 바인딩)
  → 미바인딩 claim 존재? → YES: 해당 claim 제거 + "확인 불가" 표시
                          → NO: 통과
  → output_guard(고지 부착) → 렌더
```

---

## 6. 보안 / 프라이버시 계층

> AITHOR `src/aithor_agent_framework/guard.py`(`RuntimeGuard`) 패턴 이식. 한국어 script-safe 정규식 확인됨.

### 6.1 Input Guard
- **PII 리댁션**: 주민번호·전화번호·이메일 패턴 탐지 → 입력 차단 또는 마스킹.
- **Prompt Injection 차단**: "이전 지시 무시" 류 패턴 탐지 (한국어 포함).
- **입력 최소화**: 자연어 입력에 식별정보 불필요 — UI에서 식별정보 입력 유도 안 함.

### 6.2 Tool Authorization (SSRF 방어)
- KRC API 호스트만 **allowlist** (`apis.data.go.kr`). 그 외 URL 호출 차단 (AITHOR `authorize_tool` + `_inspect_urls` 패턴).
- 서비스키는 서버 환경변수 (프론트 노출 금지).

### 6.3 프라이버시 (무저장 기본)
- 사용자 입력 **기본 무저장**. 저장 필요 시 명시적 동의 + 마스킹.
- 옵션 B(로컬 LLM) 채택 시 **외부 전송 0** — 강력한 프라이버시 차별점.
- 외부 LLM(옵션 A) 사용 시 전송 사실 고지 + 보존기간 명시.

### 6.4 저작권 / 라이선스 (자산대장, D1부터)
| 자산 | 이용조건 | 조치 |
|---|---|---|
| 논가뭄지도 | 이용허락 제한없음 | 출처표시 |
| 전원마을·농촌마을·저수지 | data.go.kr 이용조건 확인 필요 | 출처표시, 재배포 조건 확인 |
| LLM/모델 | 각 약관 | 상업이용 조건 기록 |
| 지도/폰트/이미지 | 각 라이선스 | 자산대장 관리 |

---

## 7. 점수 / 추천 로직 (결정론)

### 7.1 적합도 산식 (물 제외)
```
지구 적합도 =
    조건 매칭 점수(지역/예산/규모/선호)         · w1
  + 분양 진행단계 가중(분양중 > 예정 > 완료)      · w2
  + 마을 기본정보 적합(인구/빈집/자원)            · w3
※ 저수율·가뭄단계는 점수에 미포함 (v3 결정)
```
- 가중치(w1~w3)는 설명 가능한 고정값 (블랙박스 예측 금지).
- **저신뢰(D등급) 데이터**: 점수 계산에서 **제외**, 정보 패널에만 표시 (Codex 지적 반영 — "확인 불가"를 점수에 반영 안 함).

### 7.2 결과 정렬
- 적합도 내림차순 Top 3. 동점 시 분양율·계획세대수 순.

---

## 8. 백엔드 API 설계

### 8.1 스택
- **런타임**: Python 3.11 + FastAPI (AITHOR·minicpm와 언어 일관 + AI-research `25-backend-architect` REST 패턴).
- **HTTP 클라이언트**: httpx (타임아웃: 일반 5초, 백그라운드 60초 — FP#1).
- **구조**: Clean/Hexagonal 경계 (domain / adapters / api).

### 8.2 엔드포인트
| 메서드 | 경로 | 설명 | 요청 | 응답 |
|---|---|---|---|---|
| POST | `/api/search` | 조건 검색 | `{query|structured}` | `{top3[], evidence[], drought_panel}` |
| GET | `/api/village/{id}` | 지구 상세 | path id | `{village, sale, evidence}` |
| GET | `/api/health` | 헬스체크 | — | `{status, api_status[]}` |

### 8.3 `/api/search` 응답 스키마 (요약)
```json
{
  "top3": [{"gu_name":"...", "eupmyeon":"...", "sale_stage":"분양중",
            "sale_rate":60, "planned_households":120, "score":0.82,
            "confidence_grade":"A", "reasons":["..."]}],
  "drought_panel": {"sigungu":"...", "drought_stage":"주의",
                    "base_date":"2026-07-20", "note":"연1회 갱신, 참고용"},
  "evidence": [{"claim":"분양율 60%","api":"15104395","field":"분양율"}],
  "disclaimer": "공공데이터 기반 참고정보 · 최종 확인 필요"
}
```

---

## 9. 프론트엔드

### 9.1 스택
- React 19 (또는 경량 SPA) + 반응형. 3화면.

### 9.2 화면 명세
| 화면 | 구성요소 | 상태 |
|---|---|---|
| ① 조건 입력 | 자연어 입력창 + 예시 3개 + 필터칩(지역/예산/규모/선호) | `query`, `filters` |
| ② Top 3 카드 | 지구명·읍면동 / 진행단계·분양율(강조) / 마을 요약 / 지역 가뭄 패널 / [근거 보기] | `results`, `loading` |
| ③ 근거 리포트 | 사용 API·필드·기준일·신뢰도·유의사항·다음 행동 | `evidence`, `disclaimer` |

### 9.3 UX 원칙
- 첫 화면 = 사용자 고민 언어("귀농지, 어디가 나에게 맞을까요?"). 기술 용어 배제.
- 근거 리포트는 **보조**(더보기), 핵심은 분양 가능 지구 빠른 확인.

---

## 10. 평가 / 테스트 (AI 필요성 증빙 — Codex 지적 대응)

> AI-research `16-prompt-engineering/promptfoo`·`deepeval`, `11-evaluation/bineval` 패턴.

### 10.1 파싱 정확도 테스트셋
- **30개 대표 질의 정답셋** (`eval/intent_testset.jsonl`) → 파싱 정확도 측정.
- 지표: field-level accuracy, 유효 JSON 비율, confidence 캘리브레이션.

### 10.2 Baseline 비교 (AI 필요성 증명)
- 규칙기반 폼 입력 vs 자연어 입력: **입력 소요시간·오류율** 비교표 → "AI가 왜 필요한가"의 정량 근거.

### 10.3 환각 검증
- 모든 응답의 수치 문장이 API 필드에 바인딩되는지 자동 검사 → **환각율 0 목표**.
- deepeval faithfulness/hallucination 메트릭 (`11-evaluation`).

### 10.4 거부율
- 미지원 질의를 "확인 불가"로 정직 거부하는 비율 측정 (과잉 확신 방지).

### 10.5 CI 품질 게이트
- promptfoo CLI를 배포 전 회귀 게이트로 (파싱 정확도·환각율 임계 미달 시 배포 차단).

---

## 11. 배포 / 운영 / 관측성

### 11.1 배포
- 프론트: 정적 호스팅(Vercel 등). 백엔드: 컨테이너 or 서버리스.
- **발표용**: 라이브 URL + 로컬 실행본 + 오프라인 샘플 (3중 백업).

### 11.2 관측성 (심사 증거로 노출)
```
[운영자 검증 화면 — 발표 백오피스 1장]
- API 호출 성공/실패 로그
- 응답 시간
- fallback 발생 여부
- 사용된 데이터셋 목록 + 최신성
- 입력이 어떤 조건으로 파싱되었는지
```
> AI-research `17-observability/langfuse` 패턴(선택).

### 11.3 타임아웃/에러 정책 (FP#1)
- 일반 API 5초, 캐시 fallback 즉시. 3회 실패 → "확인 불가" 표기.

---

## 12. 참조 시스템 매핑 (실측 기반)

| 필요 기능 | 참조 시스템 | 실제 파일/스킬 | 이식 방식 |
|---|---|---|---|
| 오케스트레이션 골격 | AITHOR | `kernel.py` AgentKernel.run | **패턴 경량 이식**(전체 도입 X) |
| 도메인 유사 골격 | AITHOR | `policy_finance.py` (공공자금 검색→자격→검증→리포트) | 흐름 참조 |
| 환각 방지 근거 | AITHOR | `verifier.py`+`evidence.py` | Evidence Binding 이식 |
| 입출력 가드 | AITHOR | `guard.py` RuntimeGuard | PII/injection/SSRF allowlist 이식 |
| tier 모델 라우팅 | AITHOR | `routing.py`+`llm_providers.py` | 소형/대형 분기 참조 |
| 로컬 NL→JSON 파서 | minicpm5-poc | `harness/run_agent.py`, `convention.py` | 프라이버시 모드(옵션 B) |
| 구조화 출력 | AI-research-SKILLs | `16-.../instructor`,`outlines` | Pydantic 파싱 |
| 다단계 상태 흐름 | AI-research-SKILLs | `14-agents/langgraph` | 필요 시 |
| 환각/PII 게이트 | AI-research-SKILLs | `16-.../guardrails-ai` | 검증 보강 |
| 검색(확장) | AI-research-SKILLs | `15-rag/pgvector` | 로드맵(마을 시맨틱 검색) |
| eval 게이트 | AI-research-SKILLs | `16-.../promptfoo`,`deepeval` | CI 품질 게이트 |

> ⚠️ **과공학 방지(FP#15)**: AITHOR·langgraph 전체를 도입하지 않는다. 3주 MVP는 **단일 오케스트레이터 + 결정론 함수 + 경량 패턴 이식**. 위 표는 "패턴 출처"이지 "의존성 목록"이 아니다.
> ⚠️ **심사 노출 주의(Codex)**: 제출물(MVP제안서)에는 프레임워크명(AITHOR 등)을 노출하지 않는다. "검증 가능한 아키텍처"라는 **결과**만 제시.

---

## 13. 기술 스택 요약

| 계층 | 기술 |
|---|---|
| 프론트 | React 19 / 반응형 SPA |
| 백엔드 | Python 3.11 + FastAPI + httpx |
| AI | 호스티드 소형 LLM + structured output (기본) / MiniCPM5-1B Ollama 로컬 (확장) |
| 데이터 | KRC OpenAPI 4종 + 샘플 캐시 |
| 검증 | Evidence Binding + schema guard (AITHOR 패턴) |
| 보안 | RuntimeGuard 패턴(PII/injection/SSRF allowlist) |
| 평가 | promptfoo/deepeval CI 게이트 + 30문항 테스트셋 |
| 배포 | 정적 호스팅 + 컨테이너 + 오프라인 백업 |

---

## 14. 리스크 / 제약

| 리스크 | 영향 | 완화 |
|---|---|---|
| `county` 조회키·필드 매핑 미확인 | 저수지 보조 기능 | D1 실호출 확정, 미확정 시 저수지 기능 제외 |
| 로컬 LLM JSON 불안정 | 옵션 B 지연 | MVP는 옵션 A(호스티드), B는 확장 |
| 운영계정 심의승인(마을·전원마을) | 운영 배포 | 시연은 개발키(일 1만건) |
| 논가뭄지도 연1회 갱신 | 현재성 | 기준일 명시 + "참고용" 표기 |
| 3주 일정 | 완성도 | 4트랙 병렬 + 48h 데이터 POC 게이트(v3 §8) |

---

## 부록 A. API 필드 → 화면 매핑
| 화면 요소 | 원천 API | 필드 |
|---|---|---|
| 진행단계 배지 | 전원마을 분양정보 | 진행단계 |
| 분양율 | 전원마을 분양정보 | 분양율 |
| 계획세대수 | 전원마을 분양정보 | 계획세대수 |
| 마을 인구/빈집 | 농촌마을현황 | 인구·빈집수 |
| 지역 가뭄단계 | 논가뭄지도 | 가뭄단계·평년대비 |

## 부록 B. 출처
- data.go.kr: 15104395 / 15104291 / 15117185 / 15099919 (실측)
- 참조 시스템: `AITHOR-Agent-Framework/src/aithor_agent_framework/` (실측), `minicpm5-poc/` (실측), `AI-research-SKILLs/` catalog 390 엔트리 (실측)
- 전략 근거: `../한국농어촌공사대회_우승전략_심화재설계_v3.md`

> ⚠️ 배점표·투표 반영비율·`county` 조회키는 [미확인] — 제출 전 공식 신청서/API 실호출로 확정.
